// Поиск элементов https://learn.javascript.ru/task/find-elements

const tableAge = document.getElementById('age-table');

const tableLabels = document.querySelectorAll('#age-table label');

const firstTd = tableAge.querySelector('td');

const searchForm = document.querySelector('form[name="search"]');

const firstInput = searchForm.querySelector('input');

const  formInputs = form.querySelectorAll('input');

const lastInput = formInputs[inputs.length-1];


